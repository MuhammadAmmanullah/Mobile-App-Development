name: project_final
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


name: car_bike_customizer
description: A Flutter app for customizing cars and bikes

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^11.0.0
  provider: ^6.0.0
  audioplayers: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/turbo.mp3
    - assets/rim.png
    - assets/spoiler.png
    - assets/neon.png
